# Optional override for a FormSubmit-compatible JSON endpoint returning { success: true }.
# Default recipient: infoalphaadvisory@gmail.com (requires one-time Activate Form email).
VITE_CONTACT_ENDPOINT=
